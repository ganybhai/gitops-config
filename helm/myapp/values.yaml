replicaCount: 1
image:
  repository: ""
  tag: "latest"
  pullPolicy: Always
env: dev
service:
  type: ClusterIP
  port: 3000
resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 200m, memory: 256Mi }
